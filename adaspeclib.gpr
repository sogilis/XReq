--                         Copyright (C) 2010, Sogilis                       --
-- kate: hl Ada; indent-width 3;

with "config";

library project AdaSpecLib is

   for Library_Name     use "adaspeclib";
   for Source_Dirs      use ("src/lib");
   for Object_Dir       use "obj";
   for Library_Dir      use "lib";
   for Library_Kind     use "static";

   package Compiler is
      for Default_Switches ("Ada") use Config.Ada_Switches;
   end Compiler;

end AdaSpecLib;