<?xml version="1.0"?>
<gprconfig>
  <compiler_description>
    <name>AdaSpec</name>
  </compiler_description>
  <executable>adaspec</executable>
  <version>0.1</version>
  <languages>Feature</languages>

  <configuration>
     <compilers>
       <compiler language="Feature" />
     </compilers>
    <config>
      package Naming is
        for Body_Suffix ("Feature") use ".feature";
      end Naming;
      package Compiler is
        for Driver ("Feature") use "${PATH}adaspec";
      end Compiler;
    </config>
  </configuration>

</gprconfig>
